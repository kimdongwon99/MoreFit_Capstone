<h2>장바구니</h2>
    <% @zzim_list2.each do |c| %>
        <% if c.user_id == current_user.id %>
                <div>
                상품 - <%= c.zzim_title %>
                가격 - <%= c.zzim_price %>
                <%@cartsum+=c.zzim_price %>
                <a href="/home/zzimdelete/<%=c.id%>">--삭제하기</a>
                </div>
        <%end%>
        
    
<%end%>

총 주문가격은 <%=@cartsum %>입니다.
